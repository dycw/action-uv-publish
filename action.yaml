name: uv-publish
description: Build Python package and upload distribution

branding:
  icon: activity
  color: gray-dark

inputs:
  token:
    description: GitHub Personal Access Token
    default: ${{ github.token }}
  trusted-publishing:
    description: Configure trusted publishing
    default: false

runs:
  using: composite
  steps:
    - name: Check out repo
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Install 'uv'
      uses: astral-sh/setup-uv@v7
      with:
        python-version: 3.13

    - name: Run 'uv-publish'
      env:
        APP_TOKEN: ${{ inputs.token }}
        APP_TRUSTED_PUBLISHING: ${{ inputs.trusted-publishing }}
      run: uv run --with=dycw-uv-publish --reinstall uv-publish
      shell: bash
